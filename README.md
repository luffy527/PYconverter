# PYconverter
Python CLI tool for converting files. Handles images (for example PNG, JPG, WEBP), documents (for example PDF, DOCX, TXT, MD) and media (for example MP3, MP4, WAV) using FFmpeg, Pandoc and a PDF API. Terminal interface with progress display.
